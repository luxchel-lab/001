/**
 * Таблица товаров для мок-сервера. Один файл с приложением:
 * ../src/data/products.json (см. комментарий в src/data/products.ts —
 * значения расхода и фасовки предварительные, TBD §9 ТЗ).
 */
const PRODUCTS = require('../src/data/products.json');

module.exports = { PRODUCTS };
